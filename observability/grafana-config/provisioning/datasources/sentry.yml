apiVersion: 1

datasources:
  - name: Sentry
    uid: sentry
    type: grafana-sentry-datasource
    access: proxy
    editable: true
    jsonData:
      url: https://sentry.io
      orgSlug: timeflow-qu
    secureJsonData:
      authToken: $SENTRY_AUTH_TOKEN
